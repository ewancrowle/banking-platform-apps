on:
  push:
    paths:
      - "apps/ledger-svc/**"
      - ".github/workflows/ledger-svc.yaml"
jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v5
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26.2"
      - uses: bufbuild/buf-action@v1
        with:
          setup_only: true
      - run: buf generate apps/account-svc --template apps/account-svc/buf.gen.yaml --output apps/account-svc
      - run: buf generate apps/ledger-svc --template apps/ledger-svc/buf.gen.yaml --output apps/ledger-svc
      - name: Build
        run: go build -v apps/ledger-svc/cmd/server/main.go
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Docker meta
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}/ledger-svc
          tags: |
            # set latest tag for default branch
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          file: ./apps/ledger-svc/Dockerfile
